# ── Web Framework ──────────────────────────────────────────────────────────────
streamlit>=1.37.0

# ── LLM Inference ──────────────────────────────────────────────────────────────
ollama>=0.3.0

# ── Config & Environment ───────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0

# ── Data & Visualisation ───────────────────────────────────────────────────────
pandas>=2.0.0
plotly>=5.18.0

# ── HTTP Client (Phase 4 — Prisma AIRS cloud gates) ───────────────────────────
httpx>=0.27.0

# ── Database (Phase 0.5 — SQLite session logger) ──────────────────────────────
sqlalchemy>=2.0.0

# ── Vector Store (OMAHA Stage 2 — ChromaDB RAG) ───────────────────────────────
# Ephemeral in-process vector database for semantic retrieval.
# Embedding model runs in Ollama: ollama pull nomic-embed-text
chromadb>=0.5.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── Heavy ML / Security Scanners (Phase 3+) ────────────────────────────────────
# CPU-only PyTorch — must be listed AFTER the extra index so pip resolves correctly.
# Install via setup.bat / setup.sh, or manually:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.40.0
onnxruntime>=1.18.0

# llm-guard requires Python <=3.12 (sentencepiece wheels stop at cp312).
# Use py -3.11 or py -3.12 when creating the venv.
llm-guard>=0.3.14

# little-canary — behavioral injection probe (Hermes Labs).
# Lightweight (~5 MB, no local model weights). Requires Python 3.9+.
# Also pull the recommended canary model in Ollama:
#   ollama pull qwen2.5:1.5b
little-canary>=0.2.2
